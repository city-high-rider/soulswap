extends Resource
## This resource is any info that the Shell class might need to save when it reaches a checkpoint.
class_name ShellCheckpointSave

## Saved global transform
@export var saved_global_transform : Transform3D
